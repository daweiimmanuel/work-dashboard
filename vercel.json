{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "api/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/vercel/api.ts" }
  ]
}
